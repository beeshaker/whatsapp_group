# Add one line per client: subdomain_name  backend_port;
# Reload nginx after changes: nginx -t && systemctl reload nginx

map $client $backend_port {
    # acme      8001;
    # riverside 8002;
    # plaza     8003;
    default   8001;
}
